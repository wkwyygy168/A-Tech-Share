# @小A科技分享 专用配置
print-progress: true
concurrent: 30
timeout: 5000
min-speed: 1
download-mb: 1

rename-node: true
node-prefix: " @小A科技分享 |"

save-method: local
output-dir: "output"
output-formats:
  - yaml
  - base64

# 这里是重中之重：直接写文件名，不要加奇怪的路径
sub-urls:
  - "nodes.txt"
